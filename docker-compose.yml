services:
  api:
    build: backend/
    container_name: fastapi-application
    env_file:
      - ".env"
    ports:
      - '8000:8000'
    restart: "no"
    volumes:
      - ./backend:/app
      - /app/.venv
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload